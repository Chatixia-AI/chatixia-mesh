{
  "name": "mesh_send",
  "description": "Send a direct message to a specific agent over the WebRTC mesh network.",
  "version": "1.0.0",
  "category": "Mesh",
  "parameters": {
    "target_agent_id": {
      "type": "string",
      "description": "The ID of the agent to send the message to",
      "required": true
    },
    "message": {
      "type": "string",
      "description": "The message content to send",
      "required": true
    }
  }
}
